<div class="alert alert-success">
<h3>Logged in as:  <%= @user.name%> </h3>
<h4 ><%= flash[:notice] %></h4>
<%= link_to "Log out", logout_path, method: :destroy%>
</div>

<div>
    <table>
    <tr>
        <th>Title</th>
        <th>Author</th>
        
    </tr>
    <% @books.each do |book|%>
    <tr>
        <td><%= book.title%></td>
        <td><%= book.author%></td>
        <td><%= link_to 'View'%></td>
        <td><%= link_to 'Delete'%>
    </tr>
    <%end%>
    </table>
</div>